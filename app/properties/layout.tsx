import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "VelocityRE.pro | 50 Seller Opportunities + One Listing Guaranteed",
  description:
    "A protected-market seller opportunity program for real estate agents. Get 50 new seller opportunities each month with one listing guaranteed under written program terms.",
};

export default function PropertiesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
